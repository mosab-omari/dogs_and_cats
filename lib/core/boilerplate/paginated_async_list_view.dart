import 'package:flutter/material.dart';

class PaginatedAsyncListView extends StatelessWidget {
  const PaginatedAsyncListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
